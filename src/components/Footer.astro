---
const today = new Date();
---

<footer>
	<div class="footer-content contained">
		<p>Copyright &copy; {today.getFullYear()} Virgil Eaton. All rights reserved.</p>
	</div>
</footer>
<style>
	footer {
		margin-top: var(--spacing-xl);
		border-top: 1px solid var(--color-border);
		padding: 0.25em 1rem;
		background-color: var(--color-background-alt);
		color: var(--color-text.-muted);
		text-align: center;
		margin: 0;
		opacity: 0.8;
	}
	
	.footer-content {
		max-width: var(--spacing-width-max);
		margin: 0 auto;
		display: flex;
		flex-direction: row;
		align-items: center;
		gap: 0.5rem;
	}
	
	p {
		margin: 0;
		color: var(--color-text-muted);
	}
</style>
